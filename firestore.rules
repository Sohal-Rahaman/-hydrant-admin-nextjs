rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive development rules to support unauthenticated WhatsApp mock sign-in sessions on localhost
    match /{document=**} {
      allow read, write: if true;
    }
  }
}